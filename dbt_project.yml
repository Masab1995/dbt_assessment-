name: 'DBT_ASSESSMENT'
version: '1.0'
profile: 'default'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  DBT_ASSESSMENT:         # this matches your project name
    +schema: PUBLIC       # schema in Snowflake where tables will be created
    +materialized: table  # optional default materialisation
