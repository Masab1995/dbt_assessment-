name: 'DBT_ASSESSMENT'
version: '1.0'
profile: 'default'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  # applies to everything in the 'models' folder
  +schema: PUBLIC
  +materialized: table
